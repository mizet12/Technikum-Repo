<Window x:Class="Kontrolki.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kontrolki"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <RadioButton Content="Akcja" GroupName="Category" Checked="RadioButton_Checked"/>
            <RadioButton Content="Komedia" GroupName="Category" Checked="RadioButton_Checked"/>
            <RadioButton Content="Dramat" GroupName="Category" Checked="RadioButton_Checked"/>
            <RadioButton Content="Sci-Fi" GroupName="Category" Checked="RadioButton_Checked"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="FilmWrapPanel" Margin="10"/>
        </ScrollViewer>
    </Grid>
</Window>
