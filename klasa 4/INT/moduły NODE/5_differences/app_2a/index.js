/* NODE.JS MODULES
    Moduły lokalne (moduły zdefiniowane przez programistę).

    © Piotr Siewniak, wersja: 30.X.2021 r.
*/

const f = require('./difference');

// Wyświetlenie obiektu f:
console.log(f);

// Wywołanie funkcji fC():
f.fA();
f.fB();
f.fC();
