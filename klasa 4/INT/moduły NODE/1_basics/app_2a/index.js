/* NODE.JS MODULES
    Moduły lokalne zdefiniowane przez programistę.

    © Piotr Siewniak, wersja: 30.X.2021 r.
*/

// Dołączenie do aplikacji publicznych zasobów modułu variables:
const variables = require('./modules/variables');

console.log(variables); // wyświetlenie zawartości obiektu variables
console.log(variables.napis1);
console.log(variables.napis2);
